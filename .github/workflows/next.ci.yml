name: next > CI

on:
  push: &next_filters
    branches: [main]
    paths:
      # == Workspace configs ==
      - "**/*.just"
      - "**/justfile"
      - .prettierrc.js
      - biome.jsonc
      - bun.lock
      - bunfig.toml
      - package.json
      - tsconfig.json
      # == Package-specific ==
      - "next/**"
      - "**/next.*.yml"
  pull_request: *next_filters

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/_base.ci.yml
    with:
      package-name: next
