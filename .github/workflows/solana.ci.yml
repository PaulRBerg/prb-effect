name: solana > CI

on:
  push: &solana_filters
    branches: [main]
    paths:
      # == Workspace configs ==
      - "**/*.just"
      - "**/justfile"
      - .prettierrc.js
      - biome.jsonc
      - bun.lock
      - bunfig.toml
      - package.json
      - tsconfig.json
      # == Package-specific ==
      - "solana/**"
      - "**/solana.*.yml"
  pull_request: *solana_filters

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/_base.ci.yml
    with:
      package-name: solana
